import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};
/@type {import('next').NextConfig} */
module.exports = {
  images: {
    domains: [
      'hdtsixwiphdvzluppfqb.supabase.co',
      // any other domains you load images from…
    ],
  },
}
export default nextConfig;
